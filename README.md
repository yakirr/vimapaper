# vimapaper
Analysis and figure-making code for the paper describing the [`vima`](https://github.com/yakirr/vima/) method.

Please note: this code is intended to be run with `vima` version `0.1.7`. The newest version of `vima` has undergone API changes to make the method more usable but are not compatible with the code in these notebooks.